apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
spec:
  values:
    operatorConfig:
      hostname: karma-tailscale-operator
      defaultTags:
        - tag:karma
        - tag:k8s-operator
